import './index.scss'
import {NavLink,Link} from 'react-router-dom'
import {FontAwesomeIcon} from '@fortawesome/react-fontawesome'
import {faEnvelope,faHome,faUser} from '@fortawesome/react-fontawesome'

const NavBar = () => {
  return (
    <nav>
      <p>PlaceHolder</p>
    </nav>
  )
  
}

export default NavBar